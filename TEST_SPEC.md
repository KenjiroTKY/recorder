# 高音質ステレオICレコーダー Android アプリ テスト仕様書 (v0.1 draft)

対応する要求仕様は [SPEC.md](./SPEC.md)、実装設計は [DESIGN.md](./DESIGN.md) を参照。本書はそれらに対するテスト計画・テスト項目を定義する。

## 1. テストレベル

| レベル | 対象 | 実行方法 |
|---|---|---|
| ユニットテスト | 純粋なロジック（ハッシュ計算、ファイル分割閾値判定、WAVヘッダ計算、音質パラメータのバリデーション等）。Android依存が薄い部分。 | JUnit + `./gradlew test`（`app/src/test`）。開発フロー上、PR作成前に必ずグリーンであること。 |
| Instrumentedテスト | `AudioRecord`/`MediaCodec`/SAF等Android API依存のコンポーネント。 | `app/src/androidTest`、`./gradlew connectedAndroidTest`（エミュレータ/実機）。MVPでは重要フローのみ対象、網羅はしない。 |
| 手動実機テスト | 長時間録音、画面オフ継続、Bluetooth/通話割り込み、機種依存のステレオ/UNPROCESSED対応可否など、自動化コストが高い or 実機依存が強い項目。 | 手動チェックリスト（本書8節）。 |

## 2. 機能別テスト項目（SPEC.md対応）

### 2.1 高音質ステレオ録音（SPEC 3.1）

| # | テスト内容 | レベル |
|---|---|---|
| 2.1.1 | ステレオ対応端末で録音した波形にL/Rの独立性があること（同一信号でないこと） | 手動 |
| 2.1.2 | ステレオ非対応端末でモノラルへフォールバックし、UIにその旨表示されること | 手動 |
| 2.1.3 | 優先ソース（`UNPROCESSED`または`CAMCORDER`）が端末非対応の場合、例外を出さず代替ソースへフォールバックすること | Instrumented/手動 |
| 2.1.4 | 録音中のレベルメーター(L/R)が振幅に応じて更新されること | 手動 |
| 2.1.5 | 電子証明書OFF時は`CAMCORDER→MIC→UNPROCESSED`、ON時は`UNPROCESSED→CAMCORDER→MIC`の順でソース初期化が試行されること | Instrumented |

### 2.2 音質切替（SPEC 3.2）

| # | テスト内容 | レベル |
|---|---|---|
| 2.2.1 | 各プリセット（高音質/標準/長時間省容量/通話メモ用）で指定どおりのフォーマット・サンプルレート・bit数/ビットレートのファイルが生成されること | ユニット（パラメータ決定ロジック）＋Instrumented（実ファイル検証） |
| 2.2.2 | カスタム指定した組み合わせがすべて有効な値域内でのみ選択できること（不正値の拒否） | ユニット |
| 2.2.3 | 24bit WAV指定時、`ENCODING_PCM_FLOAT`からの変換後データが16bit/24bit範囲でクリップ・オーバーフローしないこと | ユニット |

### 2.3 保存先指定（SPEC 3.3）

| # | テスト内容 | レベル |
|---|---|---|
| 2.3.1 | `ACTION_OPEN_DOCUMENT_TREE`で選択したフォルダのURI永続化パーミッションが再起動後も保持されること | Instrumented |
| 2.3.2 | 未選択時にアプリ専用領域へフォールバックすること | ユニット/Instrumented |
| 2.3.3 | ファイル名が命名規則（`yyyyMMdd_HHmmss_<品質タグ>.拡張子`）どおりに生成されること | ユニット |

### 2.4 画面オフでの長時間録音（SPEC 3.4）

| # | テスト内容 | レベル |
|---|---|---|
| 2.4.1 | Foreground Service起動時に通知が表示され、一時停止/再開/停止が通知アクションから操作できること | Instrumented/手動 |
| 2.4.2 | 画面オフ・Doze状態でも録音が継続すること（数十分〜数時間） | 手動 |
| 2.4.3 | ファイルサイズが分割閾値（約1.8GB）に達した際に自動分割され、`_part2`,`_part3`…と連番になること | ユニット（閾値判定ロジック）＋手動（実容量での確認は時間がかかるため縮小閾値でのInstrumented代替も検討） |
| 2.4.4 | 停止時に全パートが1件の`RecordingMetadata`として合算（duration/fileSize）されること | ユニット |
| 2.4.5 | 通話やBluetooth接続変化などのAudioFocus割り込み時、設定に応じて一時停止 or 継続すること（実機でのAudioFocus奪取・復帰の確認） | 手動 |
| 2.4.5a | AudioFocus変化種別（完全喪失/一時喪失/再獲得）とユーザー設定（一時停止/継続）の組み合わせから一時停止・再開・何もしないが正しく判定されること。手動一時停止中はフォーカス再獲得しても自動再開しないこと | ユニット（`AudioFocusDecision`） |
| 2.4.6 | アプリ強制終了・クラッシュ時、直前までのデータがWAVヘッダごと有効なファイルとして残ること | 手動 |

### 2.5 タイムスタンプ付き電子証明書（SPEC 3.5）

| # | テスト内容 | レベル |
|---|---|---|
| 2.5.1 | 録音確定後、SHA-256ハッシュが正しく算出されること（既知ファイルでの期待値比較） | ユニット |
| 2.5.2 | RFC3161 TSQ生成・TSA送信・TSR受信が成功し、サイドカー(`.tsr`)が保存されること | Instrumented（テスト用TSAとしてFreeTSA.org `https://freetsa.org/tsr` への疎通で確認。既定値、SPEC.md 3.5参照） |
| 2.5.3 | ネットワーク不通時、`certificateStatus=FAILED`となり再試行キューに載ること | ユニット/Instrumented |
| 2.5.4 | 検証機能で、改ざんなしファイルは一致、改ざんありファイルは不一致と判定されること | ユニット |
| 2.5.5 | 録音一覧に証明書ステータス（付与済み/未付与/失敗・再試行待ち）が正しく表示されること | 手動 |

### 2.6 証拠性強化機能（SPEC 3.6, Phase 3）

| # | テスト内容 | レベル |
|---|---|---|
| 2.6.1 | 開始時刻証明と終了時刻証明の時刻が整合し、開始<終了の順序が成立すること（実装済み：`StartEndTimeValidatorTest`） | ユニット |
| 2.6.2 | 端末鍵署名がKeystore/StrongBox鍵で生成され、公開鍵で検証できること（検証ロジック単体は実装済み：`DeviceSignatureVerifierTest`。鍵生成・署名自体はInstrumented） | Instrumented（検証ロジック部分はユニット） |
| 2.6.3 | 区間ハッシュチェーンの一部区間のみを改ざんした場合に検知できること（実装済み：`IntervalHashChainTest`, `IntervalHashChainRecorderTest`） | ユニット |
| 2.6.4 | Chain of Custodyログの`prevEntryHash`連結が破綻した場合（改ざん）に検知できること（実装済み：`CustodyLogChainTest`） | ユニット |
| 2.6.5 | TSA証明書チェーン検証で、失効・無効な証明書を拒否できること | ユニット（モックTSA証明書使用） |
| 2.6.6 | NTP同期差分が閾値超過の場合に`clockReliability`フラグが正しく設定されること | ユニット |

### 2.7 録音の削除（SPEC 3.7）

| # | テスト内容 | レベル |
|---|---|---|
| 2.7.1 | 削除ボタンタップ時に確認ダイアログが表示され、キャンセル時は何も削除されないこと | 手動 |
| 2.7.2 | 削除確定後、音声本体および存在する全サイドカー（`.tsr`、`.chain.json`等）がSAF上から削除されること | Instrumented |
| 2.7.3 | 削除成功後、一覧表示と`recordings_index.json`からメタデータが除去されること | Instrumented |
| 2.7.4 | いずれかのファイル削除に失敗した場合、メタデータは残ったままとなり、エラーが一覧画面に表示され再試行できること | Instrumented/手動 |
| 2.7.5 | 削除成功時、Chain of Custodyログに`DELETED`エントリが追記され、`prevEntryHash`の連結が破綻しないこと | ユニット（`CustodyLogChain`） |

### 2.8 録音感度（ゲイン）調整（SPEC 3.8）

| # | テスト内容 | レベル |
|---|---|---|
| 2.8.1 | 0dB設定時、`GainProcessor`適用前後でサンプル値が変化しないこと（恒等変換） | ユニット |
| 2.8.2 | +6dB/-6dB等、dB値から線形係数への変換が正しいこと（`10^(db/20)`） | ユニット |
| 2.8.3 | ゲイン適用後の値が16bit/float双方の表現範囲を超える場合、ラップアラウンド（符号反転）せずハードクリップされること | ユニット |
| 2.8.4 | クリップが発生したバッファでは`clipped=true`、発生しないバッファでは`false`が返ること | ユニット |
| 2.8.5 | 設定値が下限-24dB〜モード別上限（CAMCORDER優先時+24dB／UNPROCESSED優先時+40dB）の範囲外にならないこと（範囲外指定時のクランプ） | ユニット |
| 2.8.6 | ゲイン設定値がDataStoreに永続化され、アプリ再起動後も保持されること | Instrumented |
| 2.8.7 | 録音中にゲインスライダーを操作すると、レベルメーターとクリッピング警告表示が即座に反映されること | 手動 |
| 2.8.8 | 設定したゲインが録音ファイルの実際の音量に反映されていること（再生して確認） | 手動 |
| 2.8.9 | 設定画面で電子証明書ON/OFFを切り替えると、ゲインスライダーの上限表示（+24dB/+40dB）が連動して変わること | 手動 |
| 2.8.10 | `GainProcessor.maxGainDb(preferUnprocessed)`がtrue時`MAX_GAIN_DB_UNPROCESSED`、false時`MAX_GAIN_DB`を返すこと | ユニット |
| 2.8.11 | `GainProcessor.autoReduceGainDb`は`clipped=false`時、現在値をそのまま返すこと | ユニット |
| 2.8.12 | `GainProcessor.autoReduceGainDb`は`clipped=true`時、デフォルトの`AUTO_REDUCTION_STEP_DB`（3dB）だけ値を下げること、および任意の`stepDb`を指定できること | ユニット |
| 2.8.13 | `GainProcessor.autoReduceGainDb`は`clipped=true`が続いても`MIN_GAIN_DB`（-24dB）でクランプされ、それ以下には下がらないこと | ユニット |
| 2.8.14 | 設定画面で「クリッピング検出時に自動でゲインを下げる」をONにして録音中にクリッピングを発生させると、ゲインスライダーの値が自動的に下がり、DataStoreにも反映されること（アプリ再起動後も保持） | 手動 |
| 2.8.15 | 上記トグルがOFFの場合、クリッピングが発生してもゲインは自動では変化しないこと（警告表示のみ） | 手動 |
| 2.8.16 | 自動でゲインが下がった後も、ユーザーが手動でスライダーを操作してゲインを上げ直せること（自動で元に戻ることはない） | 手動 |

## 3. 非機能要件テスト（SPEC 4対応）

| # | テスト内容 | レベル |
|---|---|---|
| 3.1 | 長時間録音中、他アプリのバックグラウンド処理と同時実行しても音切れが発生しないこと | 手動 |
| 3.2 | 録音中に強制終了しても、直前のフラッシュ済みデータまでは再生可能なファイルとして残ること | 手動 |
| 3.3 | 録音データ本体がネットワーク送信されないこと（証明書オプションのハッシュ値送信のみ）をネットワークログで確認 | Instrumented/手動（プロキシキャプチャ） |
| 3.4 | 日本語・英語切り替え時にUI文字列が欠落しないこと | 手動 |

## 4. テスト環境

- **必須動作確認端末**: Google Pixel 8 / Android 16（SPEC.md 2節参照）。手動テスト・実機での主要フローの確認はこの組み合わせを最低限カバーする。
- 開発フェーズではエミュレータ（API 29, 34相当）を基本とし、主要機能は実機（Pixel 8に加え可能ならメーカー違いも）で確認する。
- ステレオマイク対応可否・UNPROCESSED対応可否は機種依存が大きいため、対応表を別途整備し実機検証結果を蓄積する。

## 5. CI・実行方針

- ユニットテスト（`./gradlew test`）は開発フロー（issue→ブランチ実装→ユニットテスト→コミット→PR）の一部として、PR作成前に必ずグリーンであることを必須とする。
- Instrumentedテスト・手動テストはCI常時実行の対象外とし、Phase単位のマイルストーンでまとめて実施する。
- CI（GitHub Actions等）へのユニットテスト自動実行組み込みは今後の拡張ポイント（8節）とする。

## 6. 受け入れ基準（Definition of Done）

各issue対応は、以下を満たしてPRをマージする。

1. 該当する要件仕様書(SPEC.md)・実装仕様書(DESIGN.md)・本テスト仕様書の該当項目が更新されている
2. 追加/変更したロジックに対応するユニットテストが存在し、`./gradlew test`が通過する
3. 本書2節に該当項目がある場合、手動テストチェックリスト（8節）を実施し結果を記録している（Instrumented/手動テストが必要な変更の場合）

## 7. 今後の拡張ポイント

- Instrumentedテストの整備（現状は手動テストに依存する項目が多い）
- CIパイプラインでのユニットテスト自動実行・カバレッジ計測
- 機種別ステレオ/UNPROCESSED対応マトリクスの継続的な蓄積
- テスト用TSAモックサーバーの用意（外部TSAへの依存を減らしInstrumentedテストを安定化）

## 8. 手動テストチェックリスト（雛形）

実機検証時に使用する簡易チェックリスト。項目は2節のテスト項目IDと対応させ、結果（OK/NG/端末情報）を記録する。

| 項目ID | 端末 | OS | 結果 | 備考 |
|---|---|---|---|---|
| | | | | |
