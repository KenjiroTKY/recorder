# 高音質ステレオICレコーダー Android アプリ 仕様書 (v0.1 draft)

## 1. 概要

スマートフォンの動画撮影と同等の高音質ステレオ録音を、単体の音声レコーダーアプリとして実現する。
録音品質の切り替え、保存先の自由指定、画面オフでの長時間録音、録音ファイルへのタイムスタンプ付き電子証明書付加（オプション）をサポートする。

## 2. 対象環境

- Android ネイティブアプリ（Kotlin）
- minSdkVersion: 29 (Android 10)
- targetSdkVersion: 34 (Android 14) 相当の最新API
- スマートフォンを主対象（タブレットは副次対応）
- **動作保証端末（最低限）**: Google Pixel 8 / Android 16 を必須の動作確認対象とする。他機種・他OSバージョンは順次対応表を拡充（8節参照）。

## 3. 機能要件

### 3.1 高音質ステレオ録音

- ステレオマイク入力（`CHANNEL_IN_STEREO`）で録音。非対応機種はモノラルへフォールバックしてUIに明示する。
- 入力ソースは設定（3.5の電子証明書付与ON/OFF）に応じて切り替える。
  - **電子証明書OFF（通常時）**: 動画録画時と同様に `AudioSource.CAMCORDER` 系の入力を優先する。AGCによる音量調整が効くため、会話等でも実用的な録音音量を確保しやすい。
  - **電子証明書ON（証拠性優先時）**: AGC/ノイズサプレッサーの影響を避けるため `AudioSource.UNPROCESSED` を優先する（生音重視）。ベースの信号レベルが小さくなる分、3.8のゲイン上限を拡大して補う。
  - いずれのモードでも優先ソースが端末非対応の場合は他ソースへ自動フォールバックする。
- 録音エンジンは `AudioRecord` によるPCM直接取得を基本とし、独自にWAV/AACへエンコードする（`MediaRecorder`任せにしない）ことで、品質パラメータを完全制御する。
- 録音中はレベルメーター（L/R個別）と経過時間を表示。

### 3.2 録音フォーマット・音質切替

- **WAV (PCM非圧縮)**: 44.1/48/96kHz、16/24bit を選択可能。証拠性・高音質重視。
- **AAC-LC (M4Aコンテナ)**: 64/128/256kbpsを選択可能。容量重視。
- プリセット例:
  | プリセット | 形式 | パラメータ |
  |---|---|---|
  | 高音質 | WAV | 48kHz / 24bit |
  | 標準 | WAV | 44.1kHz / 16bit |
  | 長時間・省容量 | AAC | 128kbps |
  | 通話・メモ用 | AAC | 64kbps |
- 詳細設定画面でプリセットに縛られないカスタム指定も可能。

### 3.3 保存先指定

- **SAF (Storage Access Framework)** の `ACTION_OPEN_DOCUMENT_TREE` でユーザーが任意のフォルダ（内部ストレージ／SDカード／クラウド連携フォルダ等）を選択。
- 選択フォルダは `takePersistableUriPermission` で永続化し、以後デフォルト保存先として使用。未選択時はアプリ専用領域にフォールバック。
- ファイル命名規則: `yyyyMMdd_HHmmss_<品質タグ>.wav|m4a` （例: `20260729_143210_WAV48-24.wav`）

### 3.4 画面オフでの長時間録音

- `Foreground Service`（`FOREGROUND_SERVICE_MICROPHONE` type、Android 14+で必須）として録音を実行。
- 録音中は常時通知を表示し、一時停止・再開・停止を通知アクションから操作可能。
- 録音中のみ `PARTIAL_WAKE_LOCK` を保持し、CPUスリープによる録音停止を防止。
- バッテリー最適化除外の案内（初回のみ、任意）。
- WAVはヘッダのサイズフィールドが32bitのため上限約4GBに達する前（推奨: 2GB or 一定時間ごと）に自動でファイル分割し、連番管理する。
- 通話・Bluetooth接続変化などの `AudioFocus` 割り込みへの対応（一時停止 or 継続の方針を設定画面から選択可能。デフォルトは「一時停止」）。フォーカスが自動一時停止の原因で失われていた場合のみ、フォーカス再獲得時に自動再開する（ユーザーが手動で一時停止した場合は自動再開しない）。

### 3.5 タイムスタンプ付き電子証明書（オプション機能）

RFC 3161準拠の外部タイムスタンプ局（TSA）と連携し、録音ファイルの完全性・作成時刻を第三者証明する。

- **フロー**: 録音完了 → ファイルの SHA-256 ハッシュ算出 → RFC3161 TSQ（Timestamp Request）を生成しTSAへ送信（送信するのはハッシュ値のみ、音声本体は送信しない）→ TSR（TimeStampToken）を受信 → サイドカーファイル（例: `xxx.wav.tsr`）として同フォルダに保存。
- TSAは設定画面でエンドポイントURL・認証情報を指定可能。デフォルトは**FreeTSA.org**（`https://freetsa.org/tsr`、認証不要・無料のRFC3161準拠TSA）とし、開発・検証用途で追加設定なしに動作確認できるようにする。国内で法的証拠能力を重視する本番用途では有償の認定TSA（アマノ、セコムトラストシステムズ等）への切替を想定。
- アプリ内に**検証機能**を用意し、ファイル再ハッシュ化とトークン照合により改ざん有無を確認できるようにする。
- 直後にネットワーク不通の場合に備え、証明書取得を再送キュー化（後で自動リトライ）。
- 録音一覧に証明書ステータス（付与済み／未付与／失敗・再試行待ち）を表示。

### 3.6 証拠性強化機能（デジタルフォレンジック対応、オプション）

3.5のタイムスタンプ証明は「ハッシュ値がある時刻に存在した」ことしか証明できず、録音の生成時点の真正性やChain of Custody（証拠の連鎖）までは担保しない。法的証拠能力をより重視する用途向けに、以下を追加のオプション機能として提供する。

- **開始時刻証明**: 録音開始時点でも乱数シード等を用いてTSAへ先付けタイムスタンプを取得し、「録音がその時刻以降に開始された」ことを証明する（3.5の完了時証明とあわせて開始〜終了の時間的整合性を担保）。
- **端末鍵による電子署名**: Android Keystore（可能であればStrongBox）のハードウェア鍵で録音ファイルのハッシュに署名し、「どの端末で生成されたか」を証明できるようにする。TSAのタイムスタンプと組み合わせることで、後から作成・差し替えられた音声ファイルでないことの証明を補強する。
- **区間ハッシュチェーン**: 録音中一定間隔（例: 数十秒毎）でチャンク単位のハッシュを連結（Merkle Tree等）して記録し、録音全体のうち一部区間のみが後から差し替えられた場合に検知できるようにする。
- **Chain of Custody ログ**: 録音の生成・コピー・共有・検証等の操作履歴を、改ざん検知可能な形（例: 前エントリのハッシュを含める追記専用ログ）で記録する。
- **TSA発行者証明書チェーンの検証**: 検証機能（3.5）を拡張し、メッセージダイジェスト一致だけでなくTSA証明書の有効性・失効状態も検証する。
- **保存後の読み取り専用化**: 録音確定後はファイル属性を読み取り専用にする、またはWORM対応ストレージへの保存を案内する。
- **時刻源の信頼性表示**: 端末システムクロックがNTP等と同期しているかを確認し、録音メタデータに記録・UI表示する（時刻証明の傍証）。

### 3.7 録音の削除

- 録音一覧画面から不要な録音を個別に削除できる。削除は取り消せないため、実行前に確認ダイアログを表示し誤操作を防止する。
- 削除対象は音声本体ファイルのみでなく、同フォルダに保存された関連サイドカーファイル（電子証明書 `.tsr`、区間ハッシュチェーン `.chain.json`、および将来実装の開始時刻証明・端末鍵署名関連ファイルを含む、3.5/3.6参照）もまとめて削除し、孤立したサイドカーファイルが保存先に残らないようにする。
- 一部のサイドカーファイルの削除に失敗した場合は録音自体を「削除済み」として扱わず、録音一覧に残したままエラーを明示し再試行可能とする（メタデータと実ファイルの不整合を防ぐ）。
- 証拠性強化機能（3.6）を利用している場合、削除操作もChain of Custodyログに記録し、録音の生成から削除までの操作履歴を追跡可能にする。

### 3.8 録音感度（ゲイン）調整

- ユーザーが録音の入力感度（ゲイン）を下限 **-24dB** から、3.1の入力ソースに応じた上限まで調整できるようにする（デフォルト: 0dB＝無加工）。設定画面のスライダーで1dB単位に調整可能とし、いつでも0dBへリセットできる。
  - **CAMCORDER優先時（電子証明書OFF）**: 上限 **+24dB**（約16倍）。AGCで既にある程度レベル調整済みの入力を想定。
  - **UNPROCESSED優先時（電子証明書ON）**: 上限 **+40dB**（約100倍）。AGCによる自動レベル補正がないぶん素の信号が小さいため、会話等を実用的な音量で録音できるよう上限をさらに拡大する。
- ゲインは3.1のソース選択とは独立した、取得後のPCMサンプルに対するソフトウェア的な係数適用として実現する（AGC等の自動処理とは異なり、常にユーザー設定値どおりの一定倍率）。
- 適用したゲインは録音ファイルへの書き込みとレベルメーター（L/R）表示の両方に反映する。音質プリセット（3.2、録音開始時に固定）とは異なり、録音中でもスライダー操作を即座に反映する（リアルタイムに感度を微調整したいICレコーダーとしての利用シーンを優先）。
- **クリッピング防止**: ゲイン適用後のサンプル値が表現可能な最大値（16bit: ±32767、float: ±1.0）を超える場合はハードクリップ（値を上限/下限に丸める）し、符号反転等の破綻（ラップアラウンド）を防ぐ。
- クリッピングが発生した場合、レベルメーター付近に警告表示（例: 赤色ハイライトや「クリッピング発生」ラベル）を行い、ユーザーがゲインを下げる判断材料を提供する。警告は直近一定時間（例: 1秒）以内にクリッピングが発生したかどうかを示す簡易的なものとし、詳細な統計は取らない。

## 4. 非機能要件

- 録音中の音切れ防止（優先度の高いスレッドでのバッファ処理、リングバッファ運用）
- クラッシュ・強制終了時のデータ保護（一定間隔でのフラッシュ/ヘッダ書き戻し）
- 録音データそのものは端末外へ送信しない（外部送信はTSA向けのハッシュ値のみ）
- 日本語・英語UI対応

## 5. 主な権限

| 権限 | 用途 |
|---|---|
| RECORD_AUDIO | 録音 |
| FOREGROUND_SERVICE / FOREGROUND_SERVICE_MICROPHONE | 画面オフ長時間録音 |
| POST_NOTIFICATIONS (API33+) | 録音中通知 |
| WAKE_LOCK | 録音中のスリープ防止 |
| INTERNET | 電子証明書オプション使用時のみ、TSA通信用 |

※ SAF利用のため `READ/WRITE_EXTERNAL_STORAGE` は原則不要。

## 6. 画面構成（案）

1. **ホーム画面**: 録音開始/停止ボタン、L/Rレベルメーター（クリッピング警告表示含む）、経過時間、現在の音質設定
2. **設定画面**: 音質プリセット/詳細設定、録音感度（ゲイン）調整スライダー、保存先フォルダ選択、電子証明書ON/OFF・TSA設定
3. **録音一覧画面**: ファイル一覧、再生、証明書検証、共有・エクスポート、個別削除
4. **録音中通知**: 一時停止/再開/停止操作

## 7. アーキテクチャ概要

- Kotlin, MVVM
- `AudioRecord` によるPCM取得 → 自前WAVライター / `MediaCodec` によるAACエンコード
- `RecordingService`（Foreground Service）で録音本体を実行、UIとは `Bound Service` / `StateFlow` で連携
- 録音履歴・証明書ステータスはRoom DBまたは軽量なJSONメタデータで管理
- TSA通信はOkHttp、RFC3161のASN.1エンコード/デコードはBouncy Castle等のライブラリを検討

## 8. リスク・検討事項

- 全機種がステレオマイクに対応しているわけではない → 非対応機種はモノラル記録＋UI明示
- WAVの4GB上限 → 自動ファイル分割の仕様確定が必要
- 電子証明書向けTSAの選定（無料 vs 有償・法的証拠能力）はユーザー側での事業者選定が必要
- Android 14以降のマイク種別Foreground Service必須化、Doze/バッテリー最適化除外の案内UI設計
- UNPROCESSED音源は対応端末が限られるため、機種ごとのフォールバック挙動の検証が必要

## 9. 今後の進め方（案）

- **Phase 1 (MVP)**: 3.1〜3.4（録音・音質切替・保存先指定・長時間バックグラウンド録音）、3.8（録音感度調整）
- **Phase 2**: 3.5（電子証明書機能）
- **Phase 3**: 3.6（デジタルフォレンジック対応の証拠性強化機能）
